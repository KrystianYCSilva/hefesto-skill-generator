# Using Templates

## Overview
Hefesto uses a powerful template system to generate skills.

## Supported Variables
See `commands/helpers/variables.md` for the official list.

## Adapters
We support 7 CLIs + MCP via adapters in `commands/templates/adapters/`.
