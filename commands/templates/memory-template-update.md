# Memory Template Update
# This snippet shows how MEMORY.md should track template version

hefesto:
  version: "1.0.0"
  templates:
    version: "1.0.0"          # Matches Hefesto version
    last_updated: "{{CURRENT_TIMESTAMP}}"
    location: ".hefesto/templates/"
